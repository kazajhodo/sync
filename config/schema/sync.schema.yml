sync.settings:
  type: config_object
  label: Sync Settings
  mapping:
    log_verbose:
      type: boolean
      label: Log Verbose
    email_fail:
      type: string
      label: Email on Fail
    queue_cron_time:
      type: integer
      label: Queue cron time
